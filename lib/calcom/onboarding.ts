/**
 * Cookie set when a host chooses "I'll do this later" during Cal.com
 * onboarding. While present, the host layout stops routing them to the wizard
 * and the dashboard shows a persistent connect banner instead.
 */
export const ONBOARDING_SKIP_COOKIE = "calcom_onboarding_skipped"
